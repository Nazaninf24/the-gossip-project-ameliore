<%if current_user != nil %>
<br>
<br>
<%if @gossip_new.errors.any? %>
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
    	<p><strong>Hola muchachos !</strong>  Nous n'avons pas réussi à créer le potin pour la (ou les) raison(s) suivante(s) :</p>
    	<% @gossip_new.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>

    	<button type="button" class="close" data-dismiss="alert" aria-label="Close">
    		<span aria-hidden="true">&times;</span>
    	</button>
    </div>
    
    <%end%>

  <body>
    <!-- ... -->
    <center><h4> Créez votre nouveau sujet de potins ici :</h4>
    <br>
    <%= form_tag url_for(action: 'create'), method: "post" do %>

    <div class="form-group" style="width: 20%; margin-left: 20px;" >
      <%= label_tag 'Title' %>
      <br>
      <%= text_field_tag 'title', @gossip_new.title, class: "form-control" %>
    
      </div>
      <div class="form-group" style="width: 20%; margin-left: 20px;">
      <%= label_tag 'Content' %>
      <br>
      <%= text_area_tag 'content', @gossip_new.content, class: "form-control" %>
      <br /> 
    </div>
      <%= submit_tag "Create Gossip", class: "btn btn-primary btn-sm", style:"margin-left: 20px;"%>
    <% end %>
    <%else%>
    <div class='alert alert-danger mt-4'>
  <center><h4> Merci de vous connecter pour creer un potins</h4></center>
    </div>
  <%end%>
    <br><br>

  
  